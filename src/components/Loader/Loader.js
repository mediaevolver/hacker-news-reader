import React from 'react';

import { Animation } from './Loader_s';

const Loader = () => (
  <Animation>
    <span>.</span>
    <span>.</span>
    <span>.</span>
  </Animation>
);

export default Loader;
